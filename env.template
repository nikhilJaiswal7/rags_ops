# Vector DB Configuration
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=rag_collection
QDRANT_VECTOR_SIZE=1536

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo  # Low-cost model for generation (use gpt-4 only if needed)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small  # Separate cheap model for embeddings

# Local LLM Configuration (optional)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama2

# Redis Configuration
REDIS_URL=redis://localhost:6379
REDIS_TTL_EMBEDDINGS=86400
REDIS_TTL_RETRIEVAL=3600
REDIS_TTL_LLM=21600

# Chunking Configuration
CHUNK_SIZE=500
CHUNK_OVERLAP=50

# Retrieval Configuration
RETRIEVAL_TOP_K=10
SIMILARITY_THRESHOLD=0.7
USE_RERANKING=true

# Observability Configuration
PROMETHEUS_PORT=8000
OTLP_ENDPOINT=

# API Configuration
API_KEY=your_api_key_here
RATE_LIMIT_PER_HOUR=100

# Evaluation Configuration
# Note: PRECISION_AT_K is a list in code, but env vars don't support lists directly
# This will use the default [1,3,5] from settings.py
HALLUCINATION_THRESHOLD=0.05
TARGET_LATENCY_P95=2.0
TARGET_LATENCY_P99=3.0
TARGET_CACHE_HIT_RATE=0.7
TARGET_TOKEN_REDUCTION=0.3

# Data Paths
DATA_RAW_PATH=data/raw
DATA_PROCESSED_PATH=data/processed
DATA_MANIFEST_PATH=data/manifest.json
PROMPTS_PATH=prompts

